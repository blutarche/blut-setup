# Plugins are loaded by antidote from plugins.txt (see .zshrc).
# Do not add static source lines to Antidote cache paths here — they fail when
# the cache is empty (fresh machine, cleared ~/Library/Caches/antidote, etc.).
